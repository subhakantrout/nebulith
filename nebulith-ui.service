# Copy to /etc/systemd/system/nebulith-ui.service
# Edit User, WorkingDirectory, and ExecStart paths to match your setup
[Unit]
Description=Nebulith UI
After=network.target

[Service]
Type=simple
# CHANGE THESE to match your user and install path:
User=YOURUSER
WorkingDirectory=/home/YOURUSER/nebulith-ui
ExecStart=/home/YOURUSER/nebulith-ui/venv/bin/uvicorn app:app --port 7000 --host 0.0.0.0
Restart=always
RestartSec=3
EnvironmentFile=-/home/YOURUSER/nebulith-ui/.env

[Install]
WantedBy=multi-user.target
